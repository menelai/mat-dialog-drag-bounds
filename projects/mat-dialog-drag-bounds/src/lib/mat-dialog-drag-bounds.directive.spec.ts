import { MatDialogDragBoundsDirective } from './mat-dialog-drag-bounds.directive';

describe('MatDialogDragBoundsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDialogDragBoundsDirective();
    expect(directive).toBeTruthy();
  });
});
